A Python script that identifies the second largest unique number in a list of integers.

**Key Constraint:** This solution does **not** use sorting algorithms (`sort()` or `sorted()`) or built-in functions like `max()`, ensuring maximum performance.
